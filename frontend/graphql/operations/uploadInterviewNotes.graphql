mutation UploadInterviewNotes(
  $interviewedApplicantRecordId: ID!
  $file: Upload!
) {
  uploadInterviewNotes(
    interviewedApplicantRecordId: $interviewedApplicantRecordId
    file: $file
  ) {
    fileId
    fileName
    signedUrl
  }
}
