{
  "name": "recruitment-platform",
  "private": true,
  "scripts": {
    "codegen": "yarn --cwd backend/typescript schema:print && yarn --cwd frontend generate",
    "generate": "yarn codegen"
  }
}
